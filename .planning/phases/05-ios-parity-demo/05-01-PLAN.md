---
phase: 05-ios-parity-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/RailBridgeIOS/RailBridgeIOS.xcodeproj/project.pbxproj
  - ios/RailBridgeIOS/RailBridgeIOS/RailBridgeIOSApp.swift
  - ios/RailBridgeIOS/RailBridgeIOS/MainView.swift
  - ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeResponseFactory.swift
  - ios/RailBridgeIOS/RailBridgeIOS/SDK/RailPlusSdkAdapter.swift
  - ios/RailBridgeIOS/RailBridgeIOS/SDK/MockRailSdkAdapter.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Models/BalanceSnapshot.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Models/ChargeResult.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Models/SdkStatusSnapshot.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html
  - ios/RailBridgeIOS/RailBridgeIOSTests/BridgeResponseFactoryTests.swift
autonomous: false
requirements:
  - IOS-01
  - IOS-02
must_haves:
  truths:
    - "An Xcode-openable iOS project exists under `ios/` and can build as a standalone parity demo app."
    - "The iOS app preserves the lightweight Android-style flow: start screen first, then the WebView diagnostics screen."
    - "The page can still call `requestCharge`, `getBalance`, `getSdkStatus`, and `reportError` through a `window.RailBridge` object on iOS."
    - "Native success/error payloads keep the same baseline JSON fields and additive metadata field names already established on Android."
  artifacts:
    - path: "ios/RailBridgeIOS/RailBridgeIOS.xcodeproj/project.pbxproj"
      provides: "A runnable iOS app target and a test target for parity validation."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/RailBridgeIOSApp.swift"
      provides: "App entry point for the iOS parity demo."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/MainView.swift"
      provides: "A lightweight start screen that mirrors Android's entry flow with a `Start demo` action."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift"
      provides: "A `WKWebView` host that loads the bundled diagnostics page."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      provides: "Injected `window.RailBridge` shim wiring and base method handling for the four public actions."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeResponseFactory.swift"
      provides: "JSON payload construction aligned to Android field names."
    - path: "ios/RailBridgeIOS/RailBridgeIOSTests/BridgeResponseFactoryTests.swift"
      provides: "Contract tests for success/error/additive metadata payload shape."
  key_links:
    - from: "ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift"
      to: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      via: "The WebView host injects the bridge shim and forwards page messages to Swift handlers."
      pattern: "WKWebView|WKUserScript|railBridge"
    - from: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      to: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeResponseFactory.swift"
      via: "Native handlers build Android-shaped JSON results before calling `window.onBridgeResult(...)`."
      pattern: "status|method|data|error|retryCount|correlationId"
    - from: "ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html"
      to: "app/src/main/assets/webview/index.html"
      via: "The iOS bundle page is seeded from the Android diagnostics page so both platforms stay visually and behaviorally aligned."
      pattern: "RailBridge Demo|Diagnostics panel|window.onBridgeResult"
---

<objective>
Create the runnable iOS shell and baseline bridge contract for the parity demo without altering the current Android project.

Purpose: Give the repo an Xcode-openable iOS app that loads the same operator-facing diagnostics page and exposes the same four bridge actions through Swift.
Output: `ios/RailBridgeIOS` app target, start screen, `WKWebView` host, injected `window.RailBridge` shim, Android-shaped JSON response factory, and minimal XCTest coverage for the bridge payload contract.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-parity-demo/05-CONTEXT.md
@.planning/phases/05-ios-parity-demo/05-RESEARCH.md
@.planning/phases/05-ios-parity-demo/05-VALIDATION.md
@app/src/main/assets/webview/index.html
@app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
@app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
@app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java

<interfaces>
The iOS page contract must preserve these business actions:
```javascript
RailBridge.requestCharge(JSON.stringify({ cardId, amount }));
RailBridge.getBalance(JSON.stringify({ cardId }));
RailBridge.getSdkStatus();
RailBridge.reportError(JSON.stringify({ code, message, context }));
window.onBridgeResult(resultJson);
```

The iOS native payload contract must keep the same field names as Android:
```json
{
  "status": "success|error|received",
  "method": "requestCharge",
  "data": {},
  "error": {},
  "retryCount": 0,
  "callbackId": "cb-1",
  "correlationId": "corr-1",
  "platform": "ios",
  "stage": "js_callback",
  "durationMs": 12
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold the iOS parity app and bundled diagnostics page</name>
  <files>ios/RailBridgeIOS/RailBridgeIOS.xcodeproj/project.pbxproj, ios/RailBridgeIOS/RailBridgeIOS/RailBridgeIOSApp.swift, ios/RailBridgeIOS/RailBridgeIOS/MainView.swift, ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift, ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html</files>
  <read_first>
    - .planning/phases/05-ios-parity-demo/05-CONTEXT.md
    - .planning/phases/05-ios-parity-demo/05-RESEARCH.md
    - app/src/main/assets/webview/index.html
    - README.md
  </read_first>
  <behavior>
    - The repo gains an iOS app under `ios/RailBridgeIOS`.
    - The start screen is lightweight and exposes a `Start demo` action.
    - The demo screen hosts a `WKWebView` that loads a bundled `Resources/webview/index.html`.
    - The bundled page stays visually aligned with the Android diagnostics page.
  </behavior>
  <action>Create `ios/RailBridgeIOS/RailBridgeIOS.xcodeproj` with an app target named `RailBridgeIOS` and a test target named `RailBridgeIOSTests`. Set `PRODUCT_BUNDLE_IDENTIFIER = com.demo.railbridge.iosdemo`, `IPHONEOS_DEPLOYMENT_TARGET = 16.0`, and Swift as the project language. Add `RailBridgeIOSApp.swift` as the app entry, `MainView.swift` with a visible `Start demo` button or navigation link, and `WebViewDemoView.swift` with a `WKWebView` that loads `Resources/webview/index.html` from the app bundle. Seed `ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html` from `app/src/main/assets/webview/index.html` so the visible diagnostics page remains aligned with Android.</action>
  <acceptance_criteria>
    - `ios/RailBridgeIOS/RailBridgeIOS.xcodeproj/project.pbxproj` contains `PRODUCT_BUNDLE_IDENTIFIER = com.demo.railbridge.iosdemo;`.
    - `ios/RailBridgeIOS/RailBridgeIOS.xcodeproj/project.pbxproj` contains `IPHONEOS_DEPLOYMENT_TARGET = 16.0;`.
    - `ios/RailBridgeIOS/RailBridgeIOS/MainView.swift` contains the string `Start demo`.
    - `ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift` contains `WKWebView`.
    - `ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html` contains `RailBridge Demo` and `Diagnostics panel`.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project ios/RailBridgeIOS/RailBridgeIOS.xcodeproj -scheme RailBridgeIOS -destination "generic/platform=iOS Simulator"</automated>
  </verify>
  <done>The repo contains a buildable iOS app shell that opens the same diagnostics experience from a lightweight start screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject the iOS bridge shim and lock the base response contract</name>
  <files>ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift, ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeResponseFactory.swift, ios/RailBridgeIOS/RailBridgeIOS/SDK/RailPlusSdkAdapter.swift, ios/RailBridgeIOS/RailBridgeIOS/SDK/MockRailSdkAdapter.swift, ios/RailBridgeIOS/RailBridgeIOS/Models/ChargeResult.swift, ios/RailBridgeIOS/RailBridgeIOS/Models/BalanceSnapshot.swift, ios/RailBridgeIOS/RailBridgeIOS/Models/SdkStatusSnapshot.swift, ios/RailBridgeIOS/RailBridgeIOSTests/BridgeResponseFactoryTests.swift</files>
  <read_first>
    - .planning/phases/05-ios-parity-demo/05-CONTEXT.md
    - .planning/phases/05-ios-parity-demo/05-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
    - app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
    - app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
  </read_first>
  <behavior>
    - `window.RailBridge` exists on iOS and exposes `requestCharge`, `getBalance`, `getSdkStatus`, and `reportError`.
    - The iOS native layer can call `window.onBridgeResult(resultJson)` with Android-shaped payloads.
    - The base iOS adapter interface mirrors Android method names and model types.
    - Contract tests prove success, error, and additive metadata field names match Android expectations.
  </behavior>
  <action>Create `IOSNativeBridge.swift` that installs a `WKUserScript` at document start to define `window.RailBridge` and forward messages to a single `railBridge` script handler using method names `requestCharge`, `getBalance`, `getSdkStatus`, and `reportError`. Create `BridgeResponseFactory.swift` that emits JSON strings with exact field names `status`, `method`, `data`, `error`, `retryCount`, `callbackId`, `correlationId`, `platform`, `stage`, `durationMs`, `scenario`, `vendorCode`, `retryable`, and `resolvedByRetry`. Add Swift mirror types `ChargeResult`, `BalanceSnapshot`, `SdkStatusSnapshot`, plus a `RailPlusSdkAdapter` protocol and a baseline `MockRailSdkAdapter` with the same operation names as Android. Add `BridgeResponseFactoryTests.swift` that asserts the success/error payloads contain the exact Android-aligned keys and values.</action>
  <acceptance_criteria>
    - `ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift` contains `requestCharge`, `getBalance`, `getSdkStatus`, and `reportError`.
    - `ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift` contains `window.RailBridge`.
    - `ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeResponseFactory.swift` contains the strings `callbackId`, `correlationId`, `platform`, `stage`, and `durationMs`.
    - `ios/RailBridgeIOS/RailBridgeIOS/SDK/RailPlusSdkAdapter.swift` declares methods named `initialize`, `requestCharge`, `getBalance`, `getStatus`, and `shutdown`.
    - `ios/RailBridgeIOS/RailBridgeIOSTests/BridgeResponseFactoryTests.swift` asserts `status`, `method`, and `retryCount` for both success and error payloads.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project ios/RailBridgeIOS/RailBridgeIOS.xcodeproj -scheme RailBridgeIOS -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:RailBridgeIOSTests/BridgeResponseFactoryTests</automated>
  </verify>
  <done>The iOS app now has the same visible bridge surface and baseline payload contract as Android, giving Phase 5 a stable starting point for scenario and diagnostics parity.</done>
</task>

</tasks>

<verification>
Run the project build first, then the response-factory XCTest slice. Do not add scenario-specific diagnostics helpers in this wave beyond what the iOS bridge bootstrap needs to preserve the four business actions.
</verification>

<success_criteria>
The repo contains an Xcode-openable iOS app under `ios/`, the app loads the bundled diagnostics page from a lightweight start screen, the page sees a `window.RailBridge` object with the same four business methods as Android, and the iOS native payload contract already matches Android field names.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-parity-demo/05-01-SUMMARY.md`
</output>
