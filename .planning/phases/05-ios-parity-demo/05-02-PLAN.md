---
phase: 05-ios-parity-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioPreset.swift
  - ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioController.swift
  - ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioOutcome.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Logging/TimelineEvent.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Logging/RequestTimeline.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Logging/DiagnosticsSnapshot.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Logging/ErrorLogger.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeRequestCoordinator.swift
  - ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift
  - ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift
  - ios/RailBridgeIOS/RailBridgeIOSTests/MockRailSdkAdapterTests.swift
  - ios/RailBridgeIOS/RailBridgeIOSTests/BridgeRequestCoordinatorTests.swift
  - ios/RailBridgeIOS/RailBridgeIOSTests/DiagnosticsSnapshotTests.swift
autonomous: false
requirements:
  - IOS-01
  - IOS-02
must_haves:
  truths:
    - "The iOS demo exposes the same deterministic preset list and diagnostics field model as Android."
    - "iOS request ownership mirrors Android hardening: duplicate callbacks are suppressed, callback loss times out, and teardown abandons active ownership."
    - "The bundled diagnostics page stays usable on iOS with the same four actions and readable exported evidence."
    - "Simulator smoke demonstrates parity rather than only static file presence."
  artifacts:
    - path: "ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioPreset.swift"
      provides: "The canonical parity preset list: `normal`, `timeout`, `internal_error`, `callback_loss`, `duplicate_callback`, and `retry_exhausted`."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/Logging/DiagnosticsSnapshot.swift"
      provides: "Android-aligned structured diagnostics export with `timelines` and `inFlightRequests`."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeRequestCoordinator.swift"
      provides: "Pure Swift request ownership rules mirroring Android hardening."
    - path: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      provides: "Scenario control, diagnostics cache updates, timeout handling, duplicate suppression, and teardown-safe JS delivery."
    - path: "ios/RailBridgeIOS/RailBridgeIOSTests/BridgeRequestCoordinatorTests.swift"
      provides: "Pure Swift ownership tests for timeout, duplicate suppression, and abandonment."
  key_links:
    - from: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      to: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeRequestCoordinator.swift"
      via: "The iOS bridge delegates terminal acceptance, timeout conversion, and abandonment to the coordinator before any JS callback."
      pattern: "correlationId|callbackId|timeout|abandoned"
    - from: "ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift"
      to: "ios/RailBridgeIOS/RailBridgeIOS/Logging/ErrorLogger.swift"
      via: "Stage events, diagnostics snapshots, export JSON, and in-flight visibility all flow through the logging layer."
      pattern: "js_entry|sdk_start|timeout|sdk_callback_ignored_duplicate|inFlightRequests"
    - from: "ios/RailBridgeIOS/RailBridgeIOS/SDK/MockRailSdkAdapter.swift"
      to: "ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioController.swift"
      via: "The iOS mock adapter reads the active preset for deterministic parity with Android scenario behavior."
      pattern: "normal|timeout|internal_error|callback_loss|duplicate_callback|retry_exhausted"
---

<objective>
Mirror Android's scenario, diagnostics, and hardening behavior on iOS and prove it in the simulator.

Purpose: Finish the parity story by giving the iOS app the same preset-driven failure reproduction, structured evidence, and async ownership behavior already verified on Android.
Output: Swift scenario and logging types, request ownership coordinator, diagnostics cache updates that keep the page usable, XCTest coverage for pure Swift seams, and recorded simulator smoke for the parity flow.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-parity-demo/05-CONTEXT.md
@.planning/phases/05-ios-parity-demo/05-RESEARCH.md
@.planning/phases/05-ios-parity-demo/05-VALIDATION.md
@.planning/phases/04-android-bridge-hardening/04-VERIFICATION.md
@app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java
@app/src/main/java/com/demo/railbridge/bridge/InFlightRequestRecord.java
@app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java
@app/src/main/java/com/demo/railbridge/sdk/ScenarioPreset.java
@app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java

<interfaces>
The iOS parity layer must preserve these scenario values:
```text
normal
timeout
internal_error
callback_loss
duplicate_callback
retry_exhausted
```

The iOS diagnostics payload should stay Android-comparable:
```json
{
  "activePreset": "duplicate_callback",
  "availablePresets": ["normal", "timeout", "internal_error", "callback_loss", "duplicate_callback", "retry_exhausted"],
  "inFlightRequests": [],
  "snapshot": {
    "schemaVersion": 1,
    "timelines": []
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add iOS scenario, diagnostics, and request-ownership mirror types</name>
  <files>ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioPreset.swift, ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioController.swift, ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioOutcome.swift, ios/RailBridgeIOS/RailBridgeIOS/Logging/TimelineEvent.swift, ios/RailBridgeIOS/RailBridgeIOS/Logging/RequestTimeline.swift, ios/RailBridgeIOS/RailBridgeIOS/Logging/DiagnosticsSnapshot.swift, ios/RailBridgeIOS/RailBridgeIOS/Logging/ErrorLogger.swift, ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeRequestCoordinator.swift, ios/RailBridgeIOS/RailBridgeIOSTests/MockRailSdkAdapterTests.swift, ios/RailBridgeIOS/RailBridgeIOSTests/BridgeRequestCoordinatorTests.swift, ios/RailBridgeIOS/RailBridgeIOSTests/DiagnosticsSnapshotTests.swift</files>
  <read_first>
    - .planning/phases/05-ios-parity-demo/05-CONTEXT.md
    - .planning/phases/05-ios-parity-demo/05-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/sdk/ScenarioPreset.java
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
    - app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java
    - app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java
  </read_first>
  <behavior>
    - The iOS mock adapter uses the exact same preset names as Android.
    - The iOS diagnostics snapshot includes `schemaVersion`, `timelines`, and `inFlightRequests`.
    - Request ownership has the same effective terminal states as Android: `pending`, `success`, `error`, `timed_out`, and `abandoned`.
    - Pure Swift tests cover deterministic preset behavior, timeout single-fire, duplicate suppression, and diagnostics payload shape.
  </behavior>
  <action>Create pure Swift parity types that mirror Android semantics: `ScenarioPreset` with values `normal`, `timeout`, `internal_error`, `callback_loss`, `duplicate_callback`, and `retry_exhausted`; `ScenarioController` as the active preset store; `ScenarioOutcome` to encode duplicate, timeout, callback-loss, and retryability hints; `TimelineEvent`, `RequestTimeline`, `DiagnosticsSnapshot`, and an `ErrorLogger` ring buffer that keeps at most 50 requests; and `BridgeRequestCoordinator` with terminal states `pending`, `success`, `error`, `timed_out`, and `abandoned`. Add XCTest files `MockRailSdkAdapterTests.swift`, `BridgeRequestCoordinatorTests.swift`, and `DiagnosticsSnapshotTests.swift` that assert the exact preset names, timeout rules, duplicate suppression, and exported field names needed for Android comparison.</action>
  <acceptance_criteria>
    - `ios/RailBridgeIOS/RailBridgeIOS/SDK/ScenarioPreset.swift` contains all six preset strings.
    - `ios/RailBridgeIOS/RailBridgeIOS/Bridge/BridgeRequestCoordinator.swift` contains `pending`, `success`, `error`, `timed_out`, and `abandoned`.
    - `ios/RailBridgeIOS/RailBridgeIOS/Logging/DiagnosticsSnapshot.swift` contains `schemaVersion` and `inFlightRequests`.
    - `ios/RailBridgeIOS/RailBridgeIOSTests/BridgeRequestCoordinatorTests.swift` asserts duplicate suppression and timeout single-fire behavior.
    - `ios/RailBridgeIOS/RailBridgeIOSTests/MockRailSdkAdapterTests.swift` asserts deterministic behavior for `callback_loss` and `duplicate_callback`.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project ios/RailBridgeIOS/RailBridgeIOS.xcodeproj -scheme RailBridgeIOS -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:RailBridgeIOSTests/MockRailSdkAdapterTests -only-testing:RailBridgeIOSTests/BridgeRequestCoordinatorTests -only-testing:RailBridgeIOSTests/DiagnosticsSnapshotTests</automated>
  </verify>
  <done>The iOS app now has pure Swift mirror seams for scenario, diagnostics, and request ownership, making parity testable before full simulator wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wire diagnostics parity into the running iOS app and confirm simulator behavior</name>
  <files>ios/RailBridgeIOS/RailBridgeIOS/Bridge/IOSNativeBridge.swift, ios/RailBridgeIOS/RailBridgeIOS/WebViewDemoView.swift, ios/RailBridgeIOS/RailBridgeIOS/SDK/MockRailSdkAdapter.swift, ios/RailBridgeIOS/RailBridgeIOS/Logging/ErrorLogger.swift, ios/RailBridgeIOS/RailBridgeIOS/Resources/webview/index.html</files>
  <what-built>Pure Swift scenario, diagnostics, and request-ownership types that mirror the Android baseline, plus a runnable iOS shell and injected bridge shim.</what-built>
  <action>Update `IOSNativeBridge.swift` and `WebViewDemoView.swift` so the running iOS app supports the same diagnostics flow as Android. Wire `setScenarioPreset`, `clearDiagnostics`, `getDiagnosticsSnapshot`, and `exportDiagnostics` through the JS shim by maintaining `window.__railBridgeDiagnosticsSnapshot` and `window.__railBridgeDiagnosticsExport` from native updates. Register requests before SDK work starts, apply a 5000 ms timeout watchdog, suppress duplicate terminal callbacks after the first accepted outcome, abandon active ownership when the WebView screen is dismissed, and continue to deliver business results through `window.onBridgeResult(...)` with Android-aligned fields. Keep the visible page aligned with the bundled Android-derived `index.html` and only add iOS-specific bootstrap behavior where the transport differs.</action>
  <verify>
    <automated>xcodebuild test -project ios/RailBridgeIOS/RailBridgeIOS.xcodeproj -scheme RailBridgeIOS -destination "platform=iOS Simulator,name=iPhone 16"</automated>
  </verify>
  <how-to-verify>
    1. Build and launch `RailBridgeIOS` on an iOS Simulator using Xcode or the Build iOS Apps plugin workflow.
    2. Tap `Start demo` and confirm the WebView screen loads the diagnostics page with the same four action buttons.
    3. Select `duplicate_callback`, trigger `Request charge`, and confirm only one visible success arrives while diagnostics show ignored-duplicate evidence.
    4. Select `callback_loss`, trigger `Request charge`, wait past 5 seconds, and confirm a timeout error appears with Android-aligned fields plus no lingering in-flight request.
    5. Select `retry_exhausted` and confirm diagnostics still expose request timelines grouped by `correlationId`.
    6. Leave the WebView screen during a pending request and confirm there is no crash or stale callback into the closed screen.
    7. Open export detail and confirm the JSON includes `schemaVersion`, `timelines`, and `inFlightRequests` with Android-comparable field names.
  </how-to-verify>
  <resume-signal>Type "approved" if the iOS simulator flow matches the Android parity goals, or describe the mismatch precisely.</resume-signal>
  <done>The running iOS app demonstrates the same operator-facing diagnostics and async hardening story as Android, making the portfolio parity claim credible.</done>
</task>

</tasks>

<verification>
Run the pure Swift XCTest slice first, then the full iOS test command, then complete the simulator smoke on the same bundled diagnostics page. Do not widen the UI beyond the lightweight parity flow defined in Phase 5 context.
</verification>

<success_criteria>
The iOS app now mirrors Android's preset-driven diagnostics and request-ownership behavior closely enough that a reviewer can run the same page, trigger the same named scenarios, inspect the same field names, and see the same duplicate/timeout/teardown stabilization story on the simulator.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-parity-demo/05-02-SUMMARY.md`
</output>
