---
phase: 02-android-adapter-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java
  - app/src/main/java/com/demo/railbridge/sdk/BalanceSnapshot.java
  - app/src/main/java/com/demo/railbridge/sdk/SdkStatusSnapshot.java
  - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
  - app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
  - app/src/test/java/com/demo/railbridge/sdk/MockRailSdkAdapterTest.java
  - app/src/test/java/com/demo/railbridge/bridge/BridgeResponseFactoryTest.java
autonomous: true
requirements:
  - SDK-02
  - BRG-02
must_haves:
  truths:
    - "Android-side callers can target `RailPlusSdkAdapter` for initialize, charge, balance, status, and shutdown without importing `MockRailSdk` types."
    - "Balance and status data cross the seam as SDK-neutral snapshots instead of `MockRailSdk` nested result classes."
    - "The mock implementation remains available behind `MockRailSdkAdapter`, and JVM tests prove it preserves the current success, error, and status behavior per D-02."
    - "Bridge payload construction can emit the existing `status`, `method`, `data` or `error`, and `retryCount` fields while allowing optional metadata fields without needing Android runtime objects."
  artifacts:
    - path: "app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java"
      provides: "Typed seam with initialize, requestCharge, getBalance, getStatus, and shutdown operations per D-01."
    - path: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java"
      provides: "Wrapper that maps MockRailSdk callbacks and status into SDK-neutral bridge-facing types per D-02."
    - path: "app/src/main/java/com/demo/railbridge/sdk/BalanceSnapshot.java"
      provides: "SDK-neutral balance payload so the later bridge migration does not need MockRailSdk.BalanceResult."
    - path: "app/src/main/java/com/demo/railbridge/sdk/SdkStatusSnapshot.java"
      provides: "SDK-neutral status payload for getSdkStatus without exposing MockRailSdk internals."
    - path: "app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java"
      provides: "Android-free JSON builder that preserves current bridge fields and emits additive metadata per D-08 and D-09."
    - path: "app/src/test/java/com/demo/railbridge/sdk/MockRailSdkAdapterTest.java"
      provides: "Plain-JUnit coverage for adapter success, error, balance, and status mapping."
    - path: "app/src/test/java/com/demo/railbridge/bridge/BridgeResponseFactoryTest.java"
      provides: "Plain-JUnit coverage for backward-compatible success, error, status, and JS-report payloads."
  key_links:
    - from: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java"
      to: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdk.java"
      via: "Wrapper forwards callbacks and status requests to the existing mock implementation."
      pattern: "MockRailSdk"
    - from: "app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java"
      to: "app/src/main/assets/webview/index.html"
      via: "Payload builder keeps the existing fields used by window.onBridgeResult while adding only optional metadata."
      pattern: "status|method|data|error|retryCount"
    - from: "app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java"
      to: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      via: "02-02 can migrate the runtime bridge wiring onto the seam without reopening the SDK contract."
      pattern: "RailPlusSdkAdapter|BalanceSnapshot|SdkStatusSnapshot"
---

<objective>
Define the adapter contract, SDK-neutral result models, and backward-compatible response factory as Android-free building blocks before the runtime bridge migration.

Purpose: Establish the mock-backed seam and payload contract in plain Java first, while leaving actual `NativeBridge` and `WebViewActivity` migration for 02-02 where `SDK-01` and final `SDK-02` closure happen.
Output: Adapter contract, mock wrapper, neutral balance/status models, shared response factory, and plain-JUnit contract coverage.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-adapter-seam/02-CONTEXT.md
@.planning/phases/02-android-adapter-seam/02-RESEARCH.md
@.planning/phases/02-android-adapter-seam/02-VALIDATION.md
@app/src/main/java/com/demo/railbridge/sdk/MockRailSdk.java
@app/src/main/java/com/demo/railbridge/sdk/ChargeResult.java
@app/src/main/java/com/demo/railbridge/sdk/SdkErrorCode.java
@app/src/main/assets/webview/index.html

<interfaces>
From app/src/main/java/com/demo/railbridge/sdk/ChargeResult.java:
```java
public class ChargeResult {
    public String getTransactionId();
    public int getAmount();
    public int getBalance();
    public String getTimestamp();
}
```

From app/src/main/java/com/demo/railbridge/sdk/SdkErrorCode.java:
```java
public enum SdkErrorCode {
    ERR_SDK_INTERNAL,
    ERR_NETWORK_TIMEOUT,
    ERR_INSUFFICIENT_BALANCE,
    ERR_INVALID_CARD,
    ERR_TIMEOUT,
    ERR_UNKNOWN,
    RETRY_EXHAUSTED;

    public String getCode();
    public String getMessage();
}
```

From app/src/main/java/com/demo/railbridge/sdk/MockRailSdk.java:
```java
public void initialize(SdkCallback<Boolean> callback);
public void charge(String cardId, int amount, SdkCallback<ChargeResult> callback);
public void getBalance(String cardId, SdkCallback<BalanceResult> callback);
public void shutdown();
public String getSdkVersion();
public boolean isInitialized();
```

`window.onBridgeResult(resultJson)` in `app/src/main/assets/webview/index.html` currently branches on:
```json
{
  "status": "...",
  "method": "...",
  "data": {},
  "error": {},
  "retryCount": 0
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the adapter seam, neutral models, and Android-free adapter tests</name>
  <files>app/src/main/java/com/demo/railbridge/sdk/RailPlusSdkAdapter.java, app/src/main/java/com/demo/railbridge/sdk/BalanceSnapshot.java, app/src/main/java/com/demo/railbridge/sdk/SdkStatusSnapshot.java, app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java, app/src/test/java/com/demo/railbridge/sdk/MockRailSdkAdapterTest.java</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-android-adapter-seam/02-CONTEXT.md
    - .planning/phases/02-android-adapter-seam/02-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdk.java
    - app/src/main/java/com/demo/railbridge/sdk/ChargeResult.java
    - app/src/main/java/com/demo/railbridge/sdk/SdkErrorCode.java
  </read_first>
  <behavior>
    - Test that `MockRailSdkAdapter` forwards initialize and requestCharge success values without altering `ChargeResult`.
    - Test that `getBalance` returns `BalanceSnapshot` rather than `MockRailSdk.BalanceResult` to keep the seam real per D-01.
    - Test that adapter error callbacks and `getStatus` mapping preserve `SdkErrorCode`, initialized state, and SDK version.
  </behavior>
  <action>Create `RailPlusSdkAdapter` in `app/src/main/java/com/demo/railbridge/sdk/` with only `initialize`, `requestCharge`, `getBalance`, `getStatus`, and `shutdown` plus a nested generic `Callback<T>` per D-01. Reuse existing `ChargeResult` as the charge success model, add `BalanceSnapshot` and `SdkStatusSnapshot` as immutable SDK-neutral value objects, then implement `MockRailSdkAdapter` as the only wrapper around `MockRailSdk` per D-02. Add `MockRailSdkAdapterTest` as a plain-JUnit test using only fake callbacks or controllable test doubles so the seam is covered without `WebView`, `Handler`, or any Android runtime types. Do not claim `SDK-01` closure in this task; the runtime bridge and activity are still on old wiring until 02-02.</action>
  <acceptance_criteria>
    - `RailPlusSdkAdapter.java` declares exactly the five D-01 operations plus a nested callback type.
    - `MockRailSdkAdapter.java` is the only new class importing `MockRailSdk`, and bridge-facing model types are `ChargeResult`, `BalanceSnapshot`, and `SdkStatusSnapshot`.
    - `MockRailSdkAdapterTest` proves success, error, balance, and status mapping using plain JUnit with no Android runtime dependency.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.sdk.MockRailSdkAdapterTest"</automated>
  </verify>
  <done>The seam exists as compilable code, the mock SDK remains available behind the adapter, and Android-free tests prove the contract without pretending the bridge runtime has migrated yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the backward-compatible response factory with plain-JUnit coverage</name>
  <files>app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java, app/src/test/java/com/demo/railbridge/bridge/BridgeResponseFactoryTest.java</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-android-adapter-seam/02-CONTEXT.md
    - app/src/main/assets/webview/index.html
    - app/src/main/java/com/demo/railbridge/sdk/SdkErrorCode.java
  </read_first>
  <behavior>
    - Test that success payloads keep `status`, `method`, `data`, and `retryCount` while optionally adding `callbackId`, `correlationId`, `platform`, `stage`, and `durationMs`.
    - Test that error payloads keep `error.code`, `error.message`, and `error.retryCount`, and add optional `vendorCode` and `retryable` without removing current fields.
    - Test that status and JS-report acknowledgement payloads remain parseable by the current `window.onBridgeResult` branches in `index.html`.
  </behavior>
  <action>Create `BridgeResponseFactory` under `app/src/main/java/com/demo/railbridge/bridge/` as an Android-free helper that builds success, error, status, and JS-report acknowledgement payloads using `org.json`. Preserve the current base fields per D-08, emit new metadata only when values exist per D-09, keep top-level `retryCount` on success and status payloads, and keep nested `error.retryCount` for compatibility with the current HTML handler. Add `BridgeResponseFactoryTest` as a plain-JUnit suite. Do not wire `NativeBridge` to this helper yet; 02-02 performs the runtime migration.</action>
  <acceptance_criteria>
    - `BridgeResponseFactory.java` has one path for success, error, status, and acknowledgement payload creation.
    - `BridgeResponseFactoryTest` verifies preserved base fields and additive metadata fields for success, error, status, and JS-report acknowledgement payloads.
    - The helper and test run without `WebView`, `Handler`, or other Android runtime classes.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.bridge.BridgeResponseFactoryTest"</automated>
  </verify>
  <done>The response contract is locked by Android-free tests, so 02-02 can focus on runtime rewiring instead of inventing JSON structure during the migration.</done>
</task>

</tasks>

<verification>
Run `.\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.sdk.MockRailSdkAdapterTest" --tests "com.demo.railbridge.bridge.BridgeResponseFactoryTest"` after the plan completes. Confirm `WebViewActivity` and `NativeBridge` remain untouched in this wave so actual `SDK-01` / final `SDK-02` closure still occurs atomically in 02-02.
</verification>

<success_criteria>
`RailPlusSdkAdapter`, `MockRailSdkAdapter`, `BalanceSnapshot`, `SdkStatusSnapshot`, and `BridgeResponseFactory` exist as Android-free contracts with plain-JUnit coverage, while the runtime bridge and activity migration are intentionally deferred to 02-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-adapter-seam/02-01-SUMMARY.md`
</output>
