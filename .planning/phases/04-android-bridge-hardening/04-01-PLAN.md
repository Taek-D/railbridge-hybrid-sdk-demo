---
phase: 04-android-bridge-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java
  - app/src/main/java/com/demo/railbridge/bridge/InFlightRequestRecord.java
  - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
  - app/src/test/java/com/demo/railbridge/bridge/BridgeRequestCoordinatorTest.java
  - app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java
autonomous: true
requirements:
  - STAB-01
must_haves:
  truths:
    - "Request ownership and terminal-state decisions are testable in plain JUnit instead of being hidden entirely inside `WebView`, `Handler`, and anonymous callbacks."
    - "The first accepted terminal outcome wins for each SDK-backed request; duplicate callbacks and post-timeout callbacks are recorded as ignored evidence rather than delivered twice."
    - "Bridge-owned timeout handling converts callback loss from an indefinitely incomplete request into a visible timeout result when the bridge is still alive."
    - "Destroying the bridge abandons active request ownership and prevents later JS delivery into a dead screen."
    - "Diagnostics make current in-flight state explicit enough to inspect while preserving the existing four public WebView actions."
  artifacts:
    - path: "app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java"
      provides: "Android-free request lifecycle ownership, duplicate suppression, timeout decisions, teardown abandonment, and in-flight snapshot support."
    - path: "app/src/main/java/com/demo/railbridge/bridge/InFlightRequestRecord.java"
      provides: "Diagnostics-facing model for explicit pending request visibility."
    - path: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      provides: "Bridge wiring that registers requests, applies timeout and duplicate gates, and blocks JS delivery after teardown."
    - path: "app/src/test/java/com/demo/railbridge/bridge/BridgeRequestCoordinatorTest.java"
      provides: "Plain-JUnit coverage for duplicate suppression, timeout single-fire, teardown abandonment, and in-flight snapshots."
    - path: "app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java"
      provides: "Additive diagnostics payload coverage for explicit in-flight visibility and hardening evidence shape."
  key_links:
    - from: "app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java"
      to: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      via: "NativeBridge delegates lifecycle ownership and callback acceptance decisions to the coordinator before any JS delivery."
      pattern: "correlationId|callbackId|timeout|destroy"
    - from: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      to: "app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java"
      via: "Hardening outcomes such as timeout, duplicate-ignore, and late-callback-ignore are preserved as diagnostics evidence."
      pattern: "timeout|duplicate|late_callback|inFlight"
    - from: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      to: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java"
      via: "Phase 3 scenarios such as `duplicate_callback` and `callback_loss` remain adapter-owned, while acceptance rules move to the bridge."
      pattern: "duplicate_callback|callback_loss"
---

<objective>
Make the Android bridge resilient to duplicate callbacks, hung requests, and teardown races without changing the public WebView actions or invalidating the diagnostics work already shipped in Phase 3.

Purpose: Close the biggest stability gap in the Android demo by introducing explicit request ownership and then wiring the current bridge to respect it.
Output: A bridge-owned request coordinator, hardened `NativeBridge` wiring, additive diagnostics visibility for in-flight and ignored-late outcomes, plain-JUnit race tests, and an emulator smoke recipe for the real WebView flow.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-android-bridge-hardening/04-RESEARCH.md
@.planning/phases/04-android-bridge-hardening/04-VALIDATION.md
@.planning/phases/03-logging-and-failure-simulation/03-VERIFICATION.md
@app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
@app/src/main/java/com/demo/railbridge/retry/RetryHandler.java
@app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
@app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
@app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java

<interfaces>
The original public bridge methods remain:
```java
@JavascriptInterface public void requestCharge(String paramsJson);
@JavascriptInterface public void getBalance(String paramsJson);
@JavascriptInterface public void getSdkStatus();
@JavascriptInterface public void reportError(String errorJson);
```

Diagnostics should remain additive. Phase 4 may extend helper payloads such as:
```json
{
  "activePreset": "duplicate_callback",
  "availablePresets": ["normal", "timeout", "internal_error", "callback_loss", "duplicate_callback", "retry_exhausted"],
  "inFlightRequests": [
    {
      "correlationId": "corr-123",
      "method": "requestCharge",
      "callbackId": "cb-1",
      "state": "pending",
      "scenario": "callback_loss",
      "startedAt": "2026-04-08T10:00:00Z",
      "elapsedMs": 850
    }
  ],
  "snapshot": {
    "schemaVersion": 1,
    "timelines": []
  }
}
```

Terminal hardening outcomes may surface through existing result JSON without breaking base fields:
```json
{
  "status": "error",
  "method": "requestCharge",
  "error": {
    "code": "1005",
    "message": "Request timed out",
    "retryCount": 0
  },
  "retryCount": 0,
  "correlationId": "corr-123",
  "platform": "android",
  "stage": "timeout"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create an Android-free request ownership coordinator and tests</name>
  <files>app/src/main/java/com/demo/railbridge/bridge/BridgeRequestCoordinator.java, app/src/main/java/com/demo/railbridge/bridge/InFlightRequestRecord.java, app/src/test/java/com/demo/railbridge/bridge/BridgeRequestCoordinatorTest.java</files>
  <read_first>
    - .planning/phases/04-android-bridge-hardening/04-RESEARCH.md
    - .planning/phases/04-android-bridge-hardening/04-VALIDATION.md
    - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
    - app/src/main/java/com/demo/railbridge/retry/RetryHandler.java
  </read_first>
  <behavior>
    - Registering a request makes it visible as in-flight immediately, before any SDK callback arrives.
    - Success, error, and timeout all flow through one terminal-state gate so only the first accepted outcome wins.
    - Duplicate success or error callbacks are rejected after the first accepted terminal outcome and can be labeled as ignored evidence.
    - Timeout fires once per request and converts a still-pending request into a terminal timeout decision.
    - Destroying the bridge abandons active ownership and marks later callbacks as undeliverable.
  </behavior>
  <action>Create a plain-Java `BridgeRequestCoordinator` keyed by `correlationId` with request registration, timeout eligibility checks, terminal acceptance decisions, teardown abandonment, and diagnostics snapshot support. Add a compact `InFlightRequestRecord` model for observable pending state. Cover the coordinator with `BridgeRequestCoordinatorTest` using plain JUnit only. The coordinator must not import Android runtime classes.</action>
  <acceptance_criteria>
    - `BridgeRequestCoordinatorTest` proves duplicate suppression, timeout single-fire behavior, destroy-after-start behavior, and explicit in-flight snapshot visibility.
    - The coordinator can distinguish between accepted terminal outcomes and ignored late or duplicate callbacks.
    - The new types compile without `WebView`, `Handler`, or `Looper`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.bridge.BridgeRequestCoordinatorTest"</automated>
  </verify>
  <done>The race-condition rules are captured in an Android-free seam that execution can trust before touching runtime bridge wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire NativeBridge to the coordinator and surface hardening evidence</name>
  <files>app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java, app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java</files>
  <read_first>
    - .planning/phases/04-android-bridge-hardening/04-RESEARCH.md
    - .planning/phases/04-android-bridge-hardening/04-VALIDATION.md
    - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
    - app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
    - app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java
  </read_first>
  <behavior>
    - `requestCharge` and `getBalance` register request ownership before SDK work starts.
    - `duplicate_callback` produces one user-visible terminal result; later duplicates are recorded as ignored diagnostics evidence.
    - `callback_loss` or another hung request produces a timeout result while the bridge is still alive.
    - `destroy()` abandons active ownership, cancels outstanding timeout callbacks, and prevents late JS delivery.
    - Diagnostics helper payloads make current in-flight request state explicit without breaking the existing page contract.
  </behavior>
  <action>Integrate the coordinator into `NativeBridge` so request registration, timeout scheduling, accepted terminal delivery, duplicate-ignore handling, and teardown abandonment all flow through one ownership model. Record hardening outcomes into diagnostics with additive evidence fields or stages. Expand `NativeBridgeDiagnosticsTest` so the helper payload contract covers in-flight visibility and hardening evidence shape. Keep the four public bridge methods and the baseline result contract unchanged.</action>
  <acceptance_criteria>
    - A hanging request no longer stays pending forever when the bridge remains alive.
    - Duplicate callbacks no longer reach JS twice.
    - Teardown prevents stale JS delivery and does not rely on silent dropping alone.
    - Diagnostics make pending or abandoned request state explicit enough to inspect from the current page.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.bridge.BridgeRequestCoordinatorTest" --tests "com.demo.railbridge.bridge.NativeBridgeDiagnosticsTest"</automated>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug</automated>
    <manual>On the emulator, verify the existing four actions still work, `duplicate_callback` yields one visible result plus ignore evidence, `callback_loss` times out instead of hanging forever, and leaving the screen during a pending request does not produce a stale JS callback or crash.</manual>
  </verify>
  <done>The Android bridge is hardened against the main async ownership races while preserving the current demo shape and diagnostics surface.</done>
</task>

</tasks>

<verification>
Run the coordinator unit test first, then the diagnostics test pair, then the full `testDebugUnitTest + assembleDebug` gate from the ASCII-path repo copy. Finish with emulator smoke on the existing diagnostics page using the Phase 3 scenario controls. Do not rename or replace the original four bridge entrypoints.
</verification>

<success_criteria>
Android bridge execution has explicit request ownership, duplicate callbacks are suppressed after the first accepted outcome, callback loss becomes a timeout instead of an indefinite hang, teardown blocks stale JS delivery, and diagnostics expose current in-flight or ignored-late state without breaking the current WebView demo contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-android-bridge-hardening/04-01-SUMMARY.md`
</output>
