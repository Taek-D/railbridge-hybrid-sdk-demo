---
phase: 03-logging-and-failure-simulation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
  - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
  - app/src/main/java/com/demo/railbridge/WebViewActivity.java
  - app/src/main/assets/webview/index.html
  - app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - UI-01
must_haves:
  truths:
    - "The existing four action buttons and four bridge action methods continue to work on the same WebView page."
    - "Diagnostics controls are additive: they layer preset selection, detail viewing, and export on top of the existing flow instead of replacing it."
    - "Bridge responses keep the current base fields and add timeline-aware metadata only as optional fields."
    - "Diagnostics helpers make incomplete requests such as `callback loss` inspectable without requiring a final `window.onBridgeResult(...)` payload."
  artifacts:
    - path: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      provides: "Stage logging, diagnostics helper methods, and bridge-side scenario control wired to the new backend."
    - path: "app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java"
      provides: "Optional metadata expansion for scenario, vendor code, retryability, and duration without breaking base fields."
    - path: "app/src/main/assets/webview/index.html"
      provides: "In-page diagnostics panel, detail view, JSON export surface, and preserved four-action flow."
    - path: "app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java"
      provides: "Plain-JUnit coverage for additive diagnostics payloads and helper compatibility."
  key_links:
    - from: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      to: "app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java"
      via: "Bridge appends timeline stages and reads diagnostics snapshots keyed by the existing request context."
      pattern: "correlationId|js_entry|native_validation|sdk_start|sdk_callback|js_callback"
    - from: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      to: "app/src/main/java/com/demo/railbridge/sdk/ScenarioController.java"
      via: "Diagnostics helpers can set and read the active preset without changing the original four actions."
      pattern: "setScenario|getActive"
    - from: "app/src/main/assets/webview/index.html"
      to: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      via: "The page reads diagnostics snapshots, renders raw payload detail, and exports JSON through the new diagnostics helpers."
      pattern: "getDiagnosticsSnapshot|exportDiagnostics|clearDiagnostics"
---

<objective>
Wire the new scenario and diagnostics backend into the live Android bridge and current WebView page while preserving the existing four-button demo flow.

Purpose: Make the backend from 03-01 visible and usable in the actual running demo so the phase delivers a portfolio-ready troubleshooting surface instead of backend-only plumbing.
Output: Bridge-side stage tracing, diagnostics helper methods, additive metadata responses, in-page diagnostics UI, and a manual smoke checkpoint for the WebView flow.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logging-and-failure-simulation/03-CONTEXT.md
@.planning/phases/03-logging-and-failure-simulation/03-RESEARCH.md
@.planning/phases/03-logging-and-failure-simulation/03-VALIDATION.md
@app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
@app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
@app/src/main/java/com/demo/railbridge/WebViewActivity.java
@app/src/main/assets/webview/index.html

<interfaces>
Preserve the original page actions:
```javascript
RailBridge.requestCharge(JSON.stringify({ cardId, amount }));
RailBridge.getBalance(JSON.stringify({ cardId }));
RailBridge.getSdkStatus();
RailBridge.reportError(JSON.stringify({ code, message, context }));
window.onBridgeResult(resultJson);
```

Add diagnostics-only helpers if needed:
```java
@JavascriptInterface public void setScenarioPreset(String preset);
@JavascriptInterface public String getDiagnosticsSnapshot();
@JavascriptInterface public String exportDiagnostics();
@JavascriptInterface public void clearDiagnostics();
```

Response compatibility rule:
```json
{
  "status": "success|error|received",
  "method": "...",
  "data": {},
  "error": {},
  "retryCount": 0,
  "callbackId": "...",
  "correlationId": "...",
  "platform": "android",
  "stage": "js_callback",
  "durationMs": 12
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bridge-stage tracing and diagnostics helper methods</name>
  <files>app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java, app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java, app/src/test/java/com/demo/railbridge/bridge/NativeBridgeDiagnosticsTest.java</files>
  <read_first>
    - .planning/phases/03-logging-and-failure-simulation/03-CONTEXT.md
    - .planning/phases/03-logging-and-failure-simulation/03-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
    - app/src/main/java/com/demo/railbridge/bridge/BridgeResponseFactory.java
    - app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
  </read_first>
  <behavior>
    - Each request records `js_entry`, `native_validation`, `sdk_start`, `sdk_callback`, and `js_callback` when applicable.
    - Error and success payloads can carry scenario, vendor code, retryability, and duration as additive fields only.
    - Diagnostics helpers expose the active preset and current or export snapshot without altering the original four action methods.
  </behavior>
  <action>Update `NativeBridge` to append timeline stages through the new logging APIs and wire in diagnostics-only helper methods for scenario selection and snapshot or export retrieval. Keep the original four `@JavascriptInterface` action methods intact and route any new metadata through `BridgeResponseFactory`. Add a plain-JUnit `NativeBridgeDiagnosticsTest` that covers additive metadata construction and diagnostics-helper compatibility at the bridge-contract level without relying on a real `WebView` runtime.</action>
  <acceptance_criteria>
    - Original four action methods are still present and still named the same.
    - New diagnostics helpers are narrow and clearly separate from the business actions.
    - Stage tracing covers both completed and incomplete flows.
    - `NativeBridgeDiagnosticsTest` proves additive metadata compatibility.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.bridge.NativeBridgeDiagnosticsTest"</automated>
  </verify>
  <done>The live bridge can now drive diagnostics state and emit the evidence fields needed by the page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Extend the existing WebView page with diagnostics UI and manually confirm runtime flow</name>
  <files>app/src/main/assets/webview/index.html, app/src/main/java/com/demo/railbridge/WebViewActivity.java, app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java</files>
  <what-built>Diagnostics-aware bridge helpers, additive metadata payloads, and a backend capable of scenario control plus JSON export.</what-built>
  <action>Extend the current `index.html` page with a diagnostics panel that adds preset selection, current preset visibility, raw payload or timeline detail viewing, JSON export or copy affordance, and clear or reset controls. Preserve the current four action buttons and result log area. Only touch `WebViewActivity` if native wiring is needed for an optional export affordance.</action>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug</automated>
  </verify>
  <how-to-verify>
    1. Build and run the Android app in Android Studio or the configured emulator.
    2. Open the existing WebView demo screen and confirm the original four action buttons are still present.
    3. Use the new diagnostics panel to switch presets, then trigger `Request charge` or `Get balance` and confirm behavior matches the selected preset.
    4. Trigger `callback loss` and confirm diagnostics still show an incomplete timeline with the earlier stages even though no final result appears in `window.onBridgeResult(...)`.
    5. Trigger a retry-recovery scenario and confirm the visible log and diagnostics both show `resolvedByRetry` or equivalent evidence.
    6. Open raw detail or export view and confirm the JSON groups data by `correlationId` and includes required evidence fields.
  </how-to-verify>
  <resume-signal>Type "approved" if the diagnostics panel works on the current page and the four-action flow still feels intact, or describe the regression precisely.</resume-signal>
  <done>The current page remains familiar while now exposing a portfolio-ready diagnostics surface with scenario control and exportable evidence.</done>
</task>

</tasks>

<verification>
Before the manual checkpoint, run the full unit-plus-build gate from the ASCII-path copy. Then manually confirm the current WebView page still carries the original four actions and that diagnostics for incomplete requests are visible on the same page.
</verification>

<success_criteria>
The Android demo still uses the same page and original four actions, diagnostics controls are additive and usable, incomplete requests become visible through the new snapshot or export channel, and the running app can produce structured evidence suitable for a debugging report.
</success_criteria>

<output>
After completion, create `.planning/phases/03-logging-and-failure-simulation/03-02-SUMMARY.md`
</output>
