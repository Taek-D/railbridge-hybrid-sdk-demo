---
phase: 03-logging-and-failure-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/demo/railbridge/sdk/ScenarioPreset.java
  - app/src/main/java/com/demo/railbridge/sdk/ScenarioController.java
  - app/src/main/java/com/demo/railbridge/sdk/ScenarioOutcome.java
  - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
  - app/src/main/java/com/demo/railbridge/logging/TimelineEvent.java
  - app/src/main/java/com/demo/railbridge/logging/RequestTimeline.java
  - app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java
  - app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
  - app/src/main/java/com/demo/railbridge/logging/LogEvent.java
  - app/src/test/java/com/demo/railbridge/sdk/MockRailSdkScenarioTest.java
  - app/src/test/java/com/demo/railbridge/logging/RequestTimelineRepositoryTest.java
  - app/src/test/java/com/demo/railbridge/logging/DiagnosticsExportTest.java
autonomous: true
requirements:
  - SDK-03
  - LOG-01
  - LOG-02
must_haves:
  truths:
    - "Deterministic scenario control lives behind the adapter seam so the current bridge transport does not need another rewrite."
    - "Every request can be represented as a request timeline keyed by `correlationId`, even when no final JS callback is emitted."
    - "The export format is structured JSON organized by request timeline and includes the evidence fields locked in Phase 3 context."
    - "`retry exhausted` remains bridge-derived; the adapter only emits retryable failures that cause the bridge retry loop to end in exhaustion."
  artifacts:
    - path: "app/src/main/java/com/demo/railbridge/sdk/ScenarioPreset.java"
      provides: "Closed preset list for support-oriented failure simulation."
    - path: "app/src/main/java/com/demo/railbridge/sdk/ScenarioController.java"
      provides: "Single source of truth for the active scenario selected by diagnostics UI."
    - path: "app/src/main/java/com/demo/railbridge/sdk/ScenarioOutcome.java"
      provides: "Adapter-facing scenario metadata such as vendor code, retryability hint, and duplicate or lost callback behavior."
    - path: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java"
      provides: "Deterministic preset-driven behavior for timeout, internal error, callback loss, duplicate callback, and retry-exhausted setup."
    - path: "app/src/main/java/com/demo/railbridge/logging/TimelineEvent.java"
      provides: "Single event model for bridge and SDK stages."
    - path: "app/src/main/java/com/demo/railbridge/logging/RequestTimeline.java"
      provides: "Correlation-anchored request aggregate with completion, retry, and outcome metadata."
    - path: "app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java"
      provides: "Stable export and UI-facing diagnostics bundle."
    - path: "app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java"
      provides: "Bounded request-timeline persistence and JSON export entrypoints."
    - path: "app/src/test/java/com/demo/railbridge/sdk/MockRailSdkScenarioTest.java"
      provides: "Deterministic unit coverage for preset behavior and retry-exhausted setup."
    - path: "app/src/test/java/com/demo/railbridge/logging/RequestTimelineRepositoryTest.java"
      provides: "Coverage for stage ordering, grouping, retention, and incomplete-request visibility."
    - path: "app/src/test/java/com/demo/railbridge/logging/DiagnosticsExportTest.java"
      provides: "Coverage for JSON schema and required evidence fields."
  key_links:
    - from: "app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java"
      to: "app/src/main/java/com/demo/railbridge/retry/RetryHandler.java"
      via: "The adapter emits retryable failures for `retry exhausted`, but does not generate `RETRY_EXHAUSTED` itself."
      pattern: "ERR_NETWORK_TIMEOUT|ERR_SDK_INTERNAL|RETRY_EXHAUSTED"
    - from: "app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java"
      to: "app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java"
      via: "03-02 will append required stage events through the existing bridge flow using the new timeline APIs."
      pattern: "correlationId|stage|durationMs|scenario"
    - from: "app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java"
      to: "app/src/main/assets/webview/index.html"
      via: "03-02 can surface JSON export and raw detail viewing without redefining the request model."
      pattern: "timelines|exportedAt|schemaVersion"
---

<objective>
Introduce deterministic scenario control and the request-timeline persistence model behind the current Android bridge so failure cases become reproducible and exportable without changing the existing four public WebView actions.

Purpose: Build the backend substrate for Phase 3 first, so the UI and bridge integration in 03-02 can consume a stable scenario API and diagnostics JSON model.
Output: Scenario preset types, mock-adapter deterministic behavior, request-timeline persistence and export APIs, and plain-JUnit coverage for scenario and logging behavior.
</objective>

<execution_context>
@C:/Users/PC/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logging-and-failure-simulation/03-CONTEXT.md
@.planning/phases/03-logging-and-failure-simulation/03-RESEARCH.md
@.planning/phases/03-logging-and-failure-simulation/03-VALIDATION.md
@app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
@app/src/main/java/com/demo/railbridge/retry/RetryHandler.java
@app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
@app/src/main/java/com/demo/railbridge/logging/LogEvent.java
@app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java

<interfaces>
The original public bridge methods remain:
```java
@JavascriptInterface public void requestCharge(String paramsJson);
@JavascriptInterface public void getBalance(String paramsJson);
@JavascriptInterface public void getSdkStatus();
@JavascriptInterface public void reportError(String errorJson);
```

Phase 3 backend must prepare diagnostics data that can later surface:
```json
{
  "schemaVersion": 1,
  "exportedAt": "...",
  "timelines": [
    {
      "correlationId": "...",
      "method": "requestCharge",
      "scenario": "timeout",
      "finalStatus": "success|error|incomplete",
      "resolvedByRetry": true,
      "finalRetryCount": 1,
      "events": [
        { "stage": "js_entry", "timestamp": "...", "durationMs": 0 },
        { "stage": "native_validation", "timestamp": "...", "durationMs": 2 },
        { "stage": "sdk_start", "timestamp": "...", "durationMs": 3 }
      ]
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic scenario types and mock adapter behavior</name>
  <files>app/src/main/java/com/demo/railbridge/sdk/ScenarioPreset.java, app/src/main/java/com/demo/railbridge/sdk/ScenarioController.java, app/src/main/java/com/demo/railbridge/sdk/ScenarioOutcome.java, app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java, app/src/test/java/com/demo/railbridge/sdk/MockRailSdkScenarioTest.java</files>
  <read_first>
    - .planning/phases/03-logging-and-failure-simulation/03-CONTEXT.md
    - .planning/phases/03-logging-and-failure-simulation/03-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/sdk/MockRailSdkAdapter.java
    - app/src/main/java/com/demo/railbridge/retry/RetryHandler.java
    - app/src/main/java/com/demo/railbridge/sdk/SdkErrorCode.java
  </read_first>
  <behavior>
    - `NORMAL` preserves the existing success path.
    - `TIMEOUT` produces a retryable timeout once, then allows recovery on a later attempt.
    - `INTERNAL_ERROR` remains distinct from `RETRY_EXHAUSTED` by emitting a non-retryable bridge-visible failure while preserving vendor/internal evidence in scenario metadata.
    - `CALLBACK_LOSS` starts work but intentionally emits no callback.
    - `DUPLICATE_CALLBACK` emits the same success callback twice so observability can capture it before hardening arrives in Phase 4.
    - `RETRY_EXHAUSTED` repeatedly emits retryable failures so the bridge retry loop derives the final exhaustion outcome.
  </behavior>
  <action>Create `ScenarioPreset`, `ScenarioController`, and a small scenario outcome helper under `sdk/`, then update `MockRailSdkAdapter` to read the active preset and deterministically shape each SDK-backed request without changing the existing adapter interface. Keep the adapter transport-compatible with the current bridge and do not let it emit `RETRY_EXHAUSTED` directly. Add `MockRailSdkScenarioTest` as plain JUnit coverage for each preset, including the distinction between `INTERNAL_ERROR` and `RETRY_EXHAUSTED`.</action>
  <acceptance_criteria>
    - The active scenario can be set and read without direct `MockRailSdk` coupling.
    - Every preset produces deterministic behavior instead of randomness.
    - `RETRY_EXHAUSTED` is still derived by `RetryHandler`, not returned directly by the adapter.
    - `MockRailSdkScenarioTest` proves preset behavior with plain JUnit only.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.sdk.MockRailSdkScenarioTest"</automated>
  </verify>
  <done>Scenario control exists as a stable backend seam and the mock adapter can reproduce support-oriented failure cases on demand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace flat logs with request timelines and structured export</name>
  <files>app/src/main/java/com/demo/railbridge/logging/TimelineEvent.java, app/src/main/java/com/demo/railbridge/logging/RequestTimeline.java, app/src/main/java/com/demo/railbridge/logging/DiagnosticsSnapshot.java, app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java, app/src/main/java/com/demo/railbridge/logging/LogEvent.java, app/src/test/java/com/demo/railbridge/logging/RequestTimelineRepositoryTest.java, app/src/test/java/com/demo/railbridge/logging/DiagnosticsExportTest.java</files>
  <read_first>
    - .planning/phases/03-logging-and-failure-simulation/03-CONTEXT.md
    - .planning/phases/03-logging-and-failure-simulation/03-RESEARCH.md
    - app/src/main/java/com/demo/railbridge/logging/ErrorLogger.java
    - app/src/main/java/com/demo/railbridge/logging/LogEvent.java
    - app/src/main/java/com/demo/railbridge/bridge/NativeBridge.java
  </read_first>
  <behavior>
    - A request timeline can be created before any SDK callback arrives.
    - Stage events are append-only and ordered.
    - Export JSON is bounded, parseable, and includes `correlationId`, `scenario`, `stage`, `retryCount`, `durationMs`, `vendorCode`, `retryable`, and `resolvedByRetry` when applicable.
    - Older flat log payloads do not crash parsing or silently disappear without an intentional compatibility path.
  </behavior>
  <action>Refactor the logging layer from a flat `LogEvent` list into a bounded request-timeline store that still lives behind `ErrorLogger`. Add request-timeline and export models, keep backward compatibility for existing persisted data, and expose structured snapshot and export APIs that 03-02 can call from the bridge. Add plain JUnit tests for stage ordering, incomplete request visibility, retention limits, and JSON export schema.</action>
  <acceptance_criteria>
    - Timelines can represent both completed and incomplete requests.
    - Export JSON is organized by request timeline, not a flat event array.
    - Existing persistence does not hard-fail on older stored data.
    - New tests cover grouping, retention, and schema fields.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\codex-temp\railbridge-demo-copy && .\gradlew.bat :app:testDebugUnitTest --tests "com.demo.railbridge.logging.RequestTimelineRepositoryTest" --tests "com.demo.railbridge.logging.DiagnosticsExportTest"</automated>
  </verify>
  <done>The diagnostics backend now has a stable request-timeline store and export contract that the bridge and page can consume.</done>
</task>

</tasks>

<verification>
Run the new scenario and logging unit tests from the ASCII-path repo copy. Do not touch `index.html`, `NativeBridge`, or `WebViewActivity` in this wave beyond imports or scaffolding strictly needed by the new backend types.
</verification>

<success_criteria>
Preset-driven simulation is deterministic, request timelines exist independently of UI callbacks, export JSON preserves support-ready evidence, and the backend is covered by plain JUnit tests that run in the ASCII-path workspace copy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-logging-and-failure-simulation/03-01-SUMMARY.md`
</output>
